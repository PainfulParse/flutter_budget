class Transaction {
    final String name;
    final double amount;
    final DateTime date;
    final int categoryId

    Transaction({
        required this.name,
        required this.amount,
        required this.date,
        required this.categoryId
    })
}